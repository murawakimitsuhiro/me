{
  "private": true,
  "scripts": {
    "build": "nuxt generate",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "gh-pages -d dist -t true"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "nuxt": "3.0.0-rc.3",
    "push-dir": "^0.4.1"
  }
}
