{
  "private": true,
  "scripts": {
    "build": "nuxt generate",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "push-dir --dir=dist --branch=gh-page --cleanup && touch .output/public/.nojekyll"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.3",
    "push-dir": "^0.4.1"
  }
}
